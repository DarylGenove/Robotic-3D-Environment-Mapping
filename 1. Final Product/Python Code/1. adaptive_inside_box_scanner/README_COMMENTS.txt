These files are the same scanner project, but with the old comments removed and new beginner-friendly comments added.
Each important code line has one simple explanation on the same line.
I renamed the uploaded config(14).py to config.py because the project imports it as config.
